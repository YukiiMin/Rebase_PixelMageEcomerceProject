services:
  postgres:
    image: postgres:15-alpine
    container_name: pixelmage-postgres
    environment:
      POSTGRES_DB: ExeProject
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: PixelMage@2026
      TZ: Asia/Ho_Chi_Minh
    ports:
      - '5432:5432'
    volumes:
      - pg-data:/var/lib/postgresql/data
    networks:
      - pixelmage-net
    restart: unless-stopped

    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d ExeProject || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

    deploy:
      resources:
        limits:
          memory: 300M
          cpus: '0.5'

    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  redis:
    image: redis:7-alpine
    container_name: pixelmage-redis

    command:
      - redis-server
      - --save
      - ''
      - --appendonly
      - 'no'
      - --maxmemory
      - 128mb
      - --maxmemory-policy
      - allkeys-lru

    ports:
      - '6379:6379'

    volumes:
      - redis-data:/data

    networks:
      - pixelmage-net

    restart: unless-stopped

    deploy:
      resources:
        limits:
          memory: 150M
          cpus: '0.25'

    logging:
      driver: json-file
      options:
        max-size: '5m'
        max-file: '2'

networks:
  pixelmage-net:
    driver: bridge

volumes:
  pg-data:
  redis-data:
  n8n-data:
